<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ItemStyle" >
        <item name="android:layout_height">52dp</item>
        <item name="android:background">@color/item_background</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">@dimen/item_small_margen_left</item>
        <item name="android:paddingRight">@dimen/item_margen_right</item>
    </style>
    <style name="ItemStyle_child" parent="ItemStyle">
        <item name="android:gravity">left|center_vertical</item>
    </style>

    <style name="DialogItemStyle">
        <item name="android:gravity">center</item>
        <item name="android:layout_height">42.7dp</item>
        <item name="android:background">@drawable/selector_popupwin_item_bg</item>
        <item name="android:textColor">@drawable/selector_item_text_color</item>
        <item name="android:textSize">15sp</item>
    </style>
    <style name="DialogItemStyle_child" parent="DialogItemStyle">
        <item name="android:textColor">@color/popwin_item_text_selected</item>
    </style>

    <style name="TitleStyle">
        <item name="android:layout_height">@dimen/title_size</item>

    </style>

    <style name="RadiaButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:checked">false</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:background">@drawable/selector_radiobutton_bg</item>

    </style>

    <style name="MMListCatalog">
        <item name="android:textSize">14.0dip</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#ff595c61</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:background">@color/bg_f5f5f5</item>
        <item name="android:paddingLeft">20.0dip</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="activityDialog" parent="Theme.AppCompat.Dialog">

        //Dialog的windowFrame框为无
        <item name="android:windowFrame">@null</item>
        //是否浮现在activity之上
        <item name="android:windowIsFloating">true</item>
        //是否半透明
        <item name="android:windowIsTranslucent">true</item>
        //是否显示title
        <item name="android:windowNoTitle">true</item>
        //设置dialog的背景
        <item name="android:background">@android:color/transparent</item>
        //显示区域背景是否透明
        <item name="android:windowBackground">@null</item>
        //就是用来控制灰度的值，当为1时，界面除了我们的dialog内容是高亮显示的，dialog以外的区域是黑色的，完全看不到其他内容，系统的默认值是0.5
        <item name="android:backgroundDimAmount">0.5</item>
        //显示区域以外是否使用黑色半透明背景
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
