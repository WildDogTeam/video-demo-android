<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ItemStyle" >
        <item name="android:layout_height">41dp</item>
        <item name="android:background">@color/item_background</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingLeft">@dimen/item_margen_left</item>
        <item name="android:paddingRight">@dimen/item_margen_right</item>
    </style>

    <style name="DialogItemStyle">
        <item name="android:gravity">center</item>
        <item name="android:layout_height">42.7dp</item>
        <item name="android:background">@drawable/selector_popupwin_item_bg</item>
        <item name="android:textColor">@drawable/selector_item_text_color</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="TitleStyle">
        <item name="android:layout_height">@dimen/title_size</item>

    </style>
</resources>
